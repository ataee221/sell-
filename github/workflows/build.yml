name: Build Selective Sell

on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      
name: Checkout
      uses: actions/checkout@v4

      
name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'

      
name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4

      
name: Build mod
      run: gradle build --no-daemon

      
name: Upload JAR
      uses: actions/upload-artifact@v4
      with:
        name: SelectiveSell-JAR
        path: build/libs/*.jar
